<h1>Brands will be shown here </h1>

<% @brands.each do |brand| %>
    <h2><%= link_to brand.name, brand_cars_path(brand) %>
<% end %> 



<h1><%= link_to "Create New Brand", new_brand_path(@brands) %></h1>